# Convex backend (optional provider)

Standalone Convex backend for the CMDS Share plugin's Convex provider — an alternative to the main Next.js + Supabase server. Deploy with `npx convex deploy` from this folder, then point the plugin's Convex provider at your deployment URL.
